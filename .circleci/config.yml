version: 2
jobs:
  build:
    docker:
      - image: mono:6
    steps:
      - checkout
      - run:
          name: Restore packages
          command: dotnet restore
      - run:
          name: Build
          command: dotnet build --configuration Release --verbosity minimal SaveLockscreenImage.sln
  #test:
#dependencies:
#  override:
#    - mono nuget.exe install xunit.runner.console
workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
      #- build
      #- test:
      #    requires:
      #      - build
          filters:
            branches:
              only:
                - master
                - dev